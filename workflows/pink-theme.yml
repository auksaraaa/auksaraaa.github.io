name: 🌸 Cute Pink Theme
on:
  # เรียกใช้เมื่อมีการ push
  push:
    branches: [ main ]
  # ทำงานอัตโนมัติทุกวัน
  schedule:
    - cron: "0 0 * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # สร้าง Snake Animation สีชมพู
      - uses: Platane/snk@master
        with:
          github_user_name: ${{ auksaraaa.github.io }}
          outputs: |
            dist/github-snake-pink.svg?color_snake=ff69b4&color_dots=#ffb6c1,#ff69b4,#ff1493,#db7093,#c71585

      # GitHub Stats แบบสีชมพู
      - uses: anuraghazra/github-readme-stats@master
        with:
          show_icons: true
          theme: radical
          title_color: "ff69b4"
          icon_color: "ff1493"
          text_color: "ffb6c1"
          bg_color: "ffffff"

      # Profile Trophy สีชมพู
      - uses: ryo-ma/github-profile-trophy@v2
        with:
          theme: alduin
          title_color: "ff69b4"
          frame_color: "ff1493"
          border_color: "ffb6c1"
          no-frame: false
          column: 6
          
      # Cute Badges
      - name: Generate Cute Badges
        uses: badges/shields@v1
        with:
          style: for-the-badge
          logo: github
          color: pink
          label_color: "ff69b4"
